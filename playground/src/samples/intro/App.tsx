import './style.css'

export default function Introduction() {

    return (
        <div className="container">
            <h1>UPV AppControl Introduction</h1>
            <p>This playground will help you get started with UPV and creating Custom AppControls.</p>
            <p>In the following sections on the left, you will find introductions to various functions and systems inside the API.</p>
            <p>Due to some errors using Material UI (MUI) inside the playground, it will be unavailable. To explore using MUI for designing, use the setup from TODO to test some samples out.</p>

            <h1>To get started please pick one of the other samples int the left menu and make sure you are running this inside UPV AppControls</h1>
        </div>
    );
}